{
  "name": "domgame",
  "version": "0.1.2",
  "description": "simple dom games",
  "main": "build.js",
  "scripts": {
    "build": "babel index.js --presets=env --out-file build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke Burns",
  "license": "MIT",
  "dependencies": {
    "xoo": "^1.2.0"
  },
  "devDependencies": {
    "bel": "^6.0.0"
  }
}
